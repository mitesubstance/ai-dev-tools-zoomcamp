# Environment Configuration
# Copy this file to .env and update values as needed

# Application Environment Mode
# Options: development, production
# - development: API only mode, CORS enabled for localhost
# - production: Serves frontend static files from frontend/dist, CORS enabled as safety net
APP_ENV=development

# Server Configuration (for local development)
# HOST=0.0.0.0
# PORT=8000
